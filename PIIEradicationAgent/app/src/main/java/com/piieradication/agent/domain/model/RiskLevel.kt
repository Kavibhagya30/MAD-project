package com.piieradication.agent.domain.model

/** How sensitive/dangerous a broker's exposure is, shown on the Brokers screen and folded into the dashboard's overall privacy score. */
enum class RiskLevel { LOW, MEDIUM, HIGH }
